@model IEnumerable<Domain.Entities.Order>
@using Presentation.Helpers
@using Domain.Enums
@{
    ViewData["Title"] = "My Orders";
}

<div class="container py-4">
    <h1 class="mb-4">My Orders</h1>
    @if (Model == null || !Model.Any())
    {
        <p class="text-muted">You have no orders yet.</p>
        <a asp-controller="Shop" asp-action="Index" class="btn btn-shopwala">Start Shopping</a>
    }
    else
    {
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Order #</th>
                        <th>Date</th>
                        <th>Status</th>
                        <th>Total</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        <tr>
                            <td>@order.Id</td>
                            <td>@order.CreatedDate.ToString("dd MMM yyyy")</td>
                            <td><span class="badge @(order.Status == OrderStatus.Delivered || order.Status == OrderStatus.Completed ? "bg-success" : order.Status == OrderStatus.Cancelled ? "bg-danger" : order.Status == OrderStatus.AssignedToRider || order.Status == OrderStatus.OutForDelivery ? "bg-info" : "bg-secondary")">@OrderStatusHelper.DisplayName(order.Status)</span></td>
                            <td>Rs @order.TotalAmount.ToString("N0")</td>
                            <td><a asp-action="Details" asp-route-id="@order.Id" class="btn btn-sm btn-outline-primary">View</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
